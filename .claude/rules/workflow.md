# Workflow Rules

- 먼저 어느 레이어를 건드리는지 정합니다: `web`, `game-server`, `shared`.
- `shared`를 바꾸면 이벤트/타입 소비처를 같이 확인합니다.
- 게임 서버 수정은 가능하면 판정 로직과 transport 로직을 나눠 읽습니다.
- Phaser 씬 수정은 시각 연출과 실제 데이터 동기화를 구분해서 봅니다.
- 큰 리팩터링보다 현재 버그나 기능 범위에 맞는 수정을 우선합니다.
